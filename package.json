{
  "name": "airbnb-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server db.json --port $PORT --host 0.0.0.0",
    "build": "echo 'No build step required for JSON Server'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}